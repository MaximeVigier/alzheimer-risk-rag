# Dépendances runtime de l'API uniquement (pas ragas/langchain/umap/matplotlib,
# qui ne servent qu'aux scripts d'évaluation en eval/ et alourdiraient l'image Docker
# pour rien). Généré manuellement à partir de requirements.txt — à garder synchronisé
# si une dépendance runtime change.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
chromadb>=0.5
rank_bm25>=0.2.2
sentence-transformers>=3.0
ollama>=0.3
